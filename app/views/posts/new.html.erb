<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, placeholder: "Insert title here" %><br />
	<%= f.label :body %>
	<%= f.text_area :body, placeholder: "Insert text here" %><br />
	<%= f.submit "submit" %>
<% end %>